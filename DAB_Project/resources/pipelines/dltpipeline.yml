resources:
  pipelines:
    pipeline_neelesh_test:
      name: neelesh_test
      libraries:
        - glob:
            include: ../../resources/pipelines/neelesh_test/transformations/**
      schema: default
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog_name}
      serverless: true
      root_path: ../../resources/pipelines/neelesh_test
